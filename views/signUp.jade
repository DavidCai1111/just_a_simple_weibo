extends layout

block content
  .row
    h1.text-center 注册页
    form.form-horizontal.col-md-6.col-md-offset-3(action="/admin/signup",method="post")
      .form-group
        label.col-sm-2(for="userName") 输入用户名:
        .col-sm-10
          input#userName.form-control(type="text",name="user[name]")
      .form-group
        label.col-sm-2(for="userPassword") 输入密码:
        .col-sm-10
          input#userPassword.form-control(type="text",name="user[password]")
      .col-sm-offset-2.col-sm-10
        button.btn.btn-primary(type="submit") 注册